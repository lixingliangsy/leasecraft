import Head from 'next/head'

import { PRODUCT } from '../lib/product'
import { buildFaqJsonLd, buildHowToJsonLd } from '../lib/schema'

const SITE = 'https://leasecraft.lxsaihub.com'
const PUBLISHED = '2026-09-17'

const faqs = [
  {
    "question": "What is LeaseCraft?",
    "answer": "LeaseCraft generates plain-English lease clauses for situations like late rent, pets, and deposits, state-aware and drop-in ready."
  },
  {
    "question": "Which clauses does it cover?",
    "answer": "Common situations such as late rent, pets, and deposits."
  },
  {
    "question": "Is it legal advice?",
    "answer": "No. It is a template, not legal advice; review with counsel before use."
  },
  {
    "question": "Does it cover my state?",
    "answer": "It uses state-aware framing so the language fits your location"
  },
  {
    "question": "Who is it for?",
    "answer": "Landlords and tenants who want readable clause language to drop into an agreement."
  },
  {
    "question": "Are both sides covered?",
    "answer": "It provides landlord and tenant sides for the clause."
  }
] as { question: string; answer: string }[]

const howToBlocks = [
  {
    "name": "How to run a first pass with LeaseCraft",
    "steps": [
      {
        "name": "Open the tool",
        "text": "Go to https://leasecraft.lxsaihub.com and open the main workspace for LeaseCraft."
      },
      {
        "name": "Enter your inputs",
        "text": "Fill the fields the product asks for. Start with the shortest complete input that still reflects your real case."
      },
      {
        "name": "Generate the draft",
        "text": "Run LeaseCraft to get a structured draft you can read in plain English."
      },
      {
        "name": "Review before you act",
        "text": "Edit anything that does not match your context. Treat the output as decision support, not a final authority."
      }
    ]
  }
] as { name: string; steps: { name: string; text: string }[] }[]

type Post = { slug: string; title: string; type: string; query: string; body: string }
const posts: Post[] = [
  {
    slug: 'what-is-leasecraft',
    title: 'What is LeaseCraft?',
    type: 'Definitional · FAQPage',
    query: 'what is leasecraft',
    body: 'LeaseCraft generates plain-English lease clauses for situations like late rent, pets, and deposits, state-aware and drop-in ready.'
  },
  {
    slug: 'what-is-leasecraft-2',
    title: 'What is LeaseCraft?',
    type: 'Definitional · FAQPage',
    query: 'what is LeaseCraft',
    body: 'LeaseCraft produces plain-English lease clauses for common situations — late rent, pets, deposits — state-aware and drop-in ready. It is a template, not legal advice.'
  },
  {
    slug: 'which-clauses-does-it-cover',
    title: 'Which clauses does it cover?',
    type: 'How-to',
    query: 'which clauses does it cover',
    body: 'Common situations such as late rent, pets, and deposits.'
  },
  {
    slug: 'is-it-legal-advice',
    title: 'Is it legal advice?',
    type: 'How-to',
    query: 'is it legal advice',
    body: 'No. It is a template, not legal advice; review with counsel before use.'
  },
  {
    slug: 'does-it-cover-my-state',
    title: 'Does it cover my state?',
    type: 'How-to',
    query: 'does it cover my state',
    body: 'It uses state-aware framing so the language fits your location'
  },
  {
    slug: 'leasecraft-vs-manual',
    title: 'LeaseCraft vs doing it manually',
    type: 'Comparison',
    query: 'leasecraft vs manual',
    body: 'Use LeaseCraft when you need a fast, structured first draft aligned to its stated job. Manual work still wins for one-off edge cases or when you already have a polished template. The product removes blank-page cost; you keep final judgment.'
  }
]

export default function Page() {
  const NAME = PRODUCT.name
  return (
    <>
      <Head>
        <link rel="canonical" href={`${SITE}/blog`} />
        <meta property="og:url" content={`${SITE}/blog`} />
        <meta property="og:type" content="website" />

        <script
          type="application/ld+json"
          dangerouslySetInnerHTML={{ __html: JSON.stringify(buildFaqJsonLd(faqs)) }}
        />
        {howToBlocks.map((block, i) => (
          <script
            key={`howto-${i}`}
            type="application/ld+json"
            dangerouslySetInnerHTML={{
              __html: JSON.stringify(buildHowToJsonLd(block.name, block.steps)),
            }}
          />
        ))}
        {posts.map((p, i) => (
          <script
            key={`article-${i}`}
            type="application/ld+json"
            dangerouslySetInnerHTML={{
              __html: JSON.stringify({
                '@context': 'https://schema.org',
                '@type': 'Article',
                '@id': `${SITE}/blog`,
                mainEntityOfPage: { '@type': 'WebPage', '@id': `${SITE}/blog` },
                headline: p.title,
                description: p.body,
                inLanguage: 'en',
                datePublished: PUBLISHED,
                dateModified: PUBLISHED,
                author: { '@type': 'Organization', name: NAME, url: SITE },
                publisher: {
                  '@type': 'Organization',
                  name: NAME,
                  url: SITE,
                  logo: { '@type': 'ImageObject', url: `${SITE}/logo.png` },
                },
              }),
            }}
          />
        ))}

        <title>{`${NAME} — Blog`}</title>
        <meta name="description" content="LeaseCraft produces plain-English lease clauses for common situations — late rent, pets, deposits — state-aware and drop-in ready. It is a template, not legal a" />
      </Head>
      <div className="min-h-screen bg-slate-50 text-slate-800">
        <header className="border-b border-slate-200 bg-white">
          <div className="max-w-5xl mx-auto px-6 py-4 flex items-center justify-between">
            <a href="/" className="font-bold text-slate-900">{NAME}</a>
            <nav className="hidden md:flex gap-6 text-sm font-semibold text-slate-500">
              <a href="/use-cases" className="hover:text-slate-900">Use cases</a>
              <a href="/integrations" className="hover:text-slate-900">Integrations</a>
              <a href="/how-it-works" className="hover:text-slate-900">How it works</a>
              <a href="/security" className="hover:text-slate-900">Security</a>
              <a href="/blog" className="hover:text-slate-900">Blog</a>
            </nav>
          </div>
        </header>
        <main className="max-w-3xl mx-auto px-6 py-14">
          <h1 className="text-3xl font-bold text-slate-900">Blog</h1>
          <p className="mt-3 text-slate-600">Definitional and how-to posts that help search engines and humans understand {NAME}.</p>

          <section className="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">
            <div className="rounded-xl border border-slate-200 bg-white p-5">
              <div className="text-xs font-semibold uppercase tracking-wider text-indigo-600">What it does</div>
              <p className="mt-2 text-sm text-slate-700">{PRODUCT.description || 'LeaseCraft produces plain-English lease clauses for common situations — late rent, pets, deposits — state-aware and drop-in ready. It is a template, not legal advice.'}</p>
            </div>
            <div className="rounded-xl border border-slate-200 bg-white p-5">
              <div className="text-xs font-semibold uppercase tracking-wider text-indigo-600">Who it&apos;s for</div>
              <p className="mt-2 text-sm text-slate-700">People who need LeaseCraft's specific outcome without building the workflow from scratch.</p>
            </div>
            <div className="rounded-xl border border-slate-200 bg-white p-5">
              <div className="text-xs font-semibold uppercase tracking-wider text-indigo-600">Input → output</div>
              <p className="mt-2 text-sm text-slate-700">Enter the product inputs → review a structured plain-English draft → edit before you act.</p>
            </div>
            <div className="rounded-xl border border-slate-200 bg-white p-5">
              <div className="text-xs font-semibold uppercase tracking-wider text-indigo-600">Note</div>
              <p className="mt-2 text-sm text-slate-700">Decision-support drafts only. English final review remains a human gate.</p>
            </div>
          </section>

          <div className="mt-10 grid gap-5 sm:grid-cols-2">
          {posts.map((p) => (
            <article key={p.slug} className="rounded-xl border border-slate-200 bg-white p-6">
              <div className="text-xs font-semibold text-indigo-600 mb-1">{p.type}</div>
              <h3 className="font-semibold text-slate-900">{p.title}</h3>
              <p className="mt-2 text-sm text-slate-600">{p.body}</p>
            </article>
          ))}
          </div>
        </main>
        <footer className="border-t border-slate-200 bg-white">
          <div className="max-w-5xl mx-auto px-6 py-8 text-sm text-slate-500 flex flex-wrap gap-6">
            <a href="/security" className="hover:text-slate-900">Security</a>
            <a href="/use-cases" className="hover:text-slate-900">Use cases</a>
            <a href="/integrations" className="hover:text-slate-900">Integrations</a>
            <a href="/how-it-works" className="hover:text-slate-900">How it works</a>
            <a href="/blog" className="hover:text-slate-900">Blog</a>
          </div>
        </footer>
      </div>
    </>
  )
}
